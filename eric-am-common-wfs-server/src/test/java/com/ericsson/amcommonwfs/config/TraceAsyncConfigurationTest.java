/*
 * COPYRIGHT Ericsson 2024
 *
 *
 *
 * The copyright to the computer program(s) herein is the property of
 *
 * Ericsson Inc. The programs may be used and/or copied only with written
 *
 * permission from Ericsson Inc. or in accordance with the terms and
 *
 * conditions stipulated in the agreement/contract under which the
 *
 * program(s) have been supplied.
 */
package com.ericsson.amcommonwfs.config;

import io.micrometer.context.ContextExecutorService;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit.jupiter.SpringExtension;

import static org.junit.jupiter.api.Assertions.assertNotNull;

@ExtendWith(SpringExtension.class)
@ContextConfiguration(classes = {TraceAsyncConfiguration.class})
public class TraceAsyncConfigurationTest {

    @Autowired
    private TraceAsyncConfiguration traceAsyncConfiguration;

    @Test
    public void testExecutorParams() {
        ContextExecutorService executor = (ContextExecutorService) traceAsyncConfiguration.getAsyncExecutor();
        assertNotNull(executor);
    }

}
